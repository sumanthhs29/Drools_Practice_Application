package com.drools.coupons.repository;

import com.drools.coupons.dto.Order;
import org.springframework.stereotype.Repository;

@Repository
public class CartRepo {
//    public Order getOrderDetails(Order order){
//       return new Order(order.getProducts(),order.getShippingDetails(),
//               order.getCoupons(), order.getCustomer(), order.getTotalAmount());
//    }
}
